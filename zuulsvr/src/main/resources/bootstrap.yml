spring:
  application:
    name: zuulservice
  profiles:
    active:
      default
  cloud:
    config:
      enabled: true

#probavanje da bi radio round robin
      
#eureka:
#  instance:
#    instance-id: 
#      ${spring.application.name}:${spring.application.instance_id:${random.value}}
    
